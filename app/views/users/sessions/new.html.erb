<div class="container">
  <div class="row mt-3 mb-3">
    <div class="col-lg-10 mx-auto">
      <h2>ログイン</h2>
      <h4>会員の方はこちらからログイン</h4>
        <%= form_with model: @user, url: user_session_path, local: true do |f| %>
          <table class="table table-borderless col-md-6 mx-auto">
            <tr>
              <th style="width: 30%"><%= f.label 'メールアドレス' %></th>
              <td><%= f.text_field :email, autofocus: true, placeholder: "sample@sample.com", autocomplete: "email_regexp", class: "form-control-sm col-12" %></td>
            </tr>
            <tr>
              <th><%= f.label 'パスワード' %></th>
              <td><%= f.password_field :password, placeholder: "パスワード", autocomplete: "current-password", class: "form-control-sm col-12" %></td>
            </tr>
          </table>
          <div class="actions text-center">
            <%= f.submit "ログイン", class: "btn btn-success col-4 mt-2" %>
          </div>
        <% end %>

      <div class="my-3">
        <h4 class="mb-3">ユーザー登録がお済みでない方</h4>
        <%= link_to "こちら", new_user_registration_path %> から新規登録を行ってください。
      </div>
    </div>
  </div>
</div>